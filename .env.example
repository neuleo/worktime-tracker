# .env.example - Environment variables for the Arbeitszeit Tracker application.
#
# This file should be copied to .env and configured with your actual secrets.
# DO NOT commit .env to your Git repository!

# Password for the application login.
# Replace 'your_app_password' with a strong, unique password.
APP_PASSWORD=your_app_password

# Secret key for signing JWTs (JSON Web Tokens).
# This should be a long, random string.
# You can generate one using: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key

# Secret key for the MacroDroid webhook.
# This should also be a long, random string, unique from JWT_SECRET_KEY.
# You can generate one using: openssl rand -hex 32
STAMP_WEBHOOK_SECRET=your_webhook_secret
